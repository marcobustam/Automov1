@page
@model Automov1.Pages.Mantenedores.Modelos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Modelo[0].NombreModelo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Modelo) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreModelo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ModeloID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ModeloID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ModeloID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
